<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="1dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintDimensionRatio="W,1:1"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/imgThumbnail"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:src="@drawable/gradient"
        app:layout_constraintDimensionRatio="W,1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:drawableLeft="@drawable/ic_play"
        android:gravity="center_vertical"
        android:id="@+id/txtVidDur"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/rail_med"
        android:layout_marginStart="@dimen/img_album_margin_start"
        android:layout_marginBottom="@dimen/img_album_margin_start"
        android:text=" 00:00 "
        android:textColor="@color/color_grey"
        android:textSize="@dimen/txt_song_desc_size"
        app:layout_constraintBottom_toBottomOf="@+id/imgThumbnail"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>